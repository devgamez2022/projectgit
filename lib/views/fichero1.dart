import 'package:flutter/material.dart';

class Fichero1 extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Fichero One'),
      ),
      body: Column(
        children: [

        ],
      ),
    );
  }

}